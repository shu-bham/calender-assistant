# Calender Assistant App

# Build project

```sh
$ gradlew build
```

# App Run

```sh
$ gradlew bootRun
```

# Swagger-UI
Access the endpoints using swagger UI by accessing `http://localhost:8080/swagger-ui.html`
